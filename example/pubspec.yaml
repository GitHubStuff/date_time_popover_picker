name: xample
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1

environment:
  sdk: ">=2.13.4 <3.0.0" #Dart SDK 1-JUL-2021
  flutter: ">=2.2.3"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  date_timer_picker_widget:
    path: ../
  cupertino_icons: ^1.0.3
  # flutter_classes:
  #   git:
  #     url: https://github.com/GitHubStuff/flutter_classes
  flutter_bloc: ^7.0.1
  flutter_modular: ^3.3.1
  intl: ^0.17.0
  # theme_manager:
  #   git:
  #     url: https://github.com/GitHubStuff/theme_manager

dev_dependencies:
  test: ^1.17.10

dependency_overrides:
  flutter_classes:
    git:
      url: https://github.com/GitHubStuff/flutter_classes
#   theme_manager:
#     git:
#       url: https://github.com/GitHubStuff/theme_manager


flutter:
  uses-material-design: true
  generate: true
