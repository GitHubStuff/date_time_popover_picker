name: xample
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.3

environment:
  sdk: ">=2.14.2 <3.0.0" #Dart SDK 19-SEP-2021
  flutter: ">=2.5.1"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  date_time_popover_picker:
    path: ../
  cupertino_icons: ^1.0.3
  flutter_bloc: ^7.3.0
  flutter_modular: ^4.0.0+7
  intl: ^0.17.0

dev_dependencies:
  test: ^1.17.10

dependency_overrides:

flutter:
  uses-material-design: true
  generate: true
